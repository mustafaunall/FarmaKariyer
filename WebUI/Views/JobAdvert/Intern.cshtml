@using Domain.Model;
@using System.Globalization;
@model List<Advert>?

@{
    ViewData["Title"] = "Teknisyen İlanları";
    int page = ViewBag.Page ?? 1;
    int pageCount = ViewBag.PageCount ?? 1;
    Domain.ViewModel.Core.PaginationVM pagingVm = new()
            {
                Controller = "JobAdvert",
                Action = "Intern",
                Page = page,
                PageCount = pageCount
            };
}

<!--Start breadcrumb area-->
<section class="breadcrumb-area" style="background-image: url(/images/sss.png);">
    <div class="container text-center">
        <h1>Stajyer İlanları</h1>
    </div>
</section>
<!--End breadcrumb area-->
<!--Start breadcrumb bottom area-->
<section class="breadcrumb-botton-area">
    <div class="container">
        <div class="left pull-left">
            <ul>
                <li><a href="@Url.Action("Index","Home")">Anasayfa</a></li>
                <li><i class="fa fa-angle-right" style="color:black;" aria-hidden="true"></i></li>
                <li><a href="index.html">İlanlar</a></li>
                <li><i class="fa fa-angle-right" aria-hidden="true"></i></li>
                <li>Stajyer İlanları</li>
            </ul>
        </div>
    </div>
</section>
<!--End breadcrumb bottom area-->
<!--Start blog area-->
<section id="shop-area" class="main-shop-area">
    <div class="container">
        <div class="row">
            <!--Start sidebar Wrapper-->
            <div class="col-lg-3 col-md-4 col-sm-7 col-xs-12">
                <div class="sidebar-wrapper">
                    <div class="single-sidebar">
                        <div class="sec-title">
                            <h3>Lokasyon</h3>
                            <span class="border"></span>
                        </div>
                        <ul class="categories clearfix">
                            <li>
                                <select id="Iller" class="form-control" name="Province">
                                    <option disabled value="0">İl Seçiniz</option>
                                </select>
                            </li>
                            <li>
                                <select id="Ilceler" class="form-control" name="District">
                                    <option disabled value="0">Lütfen Önce bir İl seçiniz</option>
                                </select>
                            </li>
                        </ul>
                    </div>
                    <script src="~/scripts/ililce.js" province="0" district="0"></script> <!--il-ilçe-script-->
                    <div class="single-sidebar">
                        <div class="sec-title">
                            <h3>Stajyer Tipi</h3>
                            <span class="border"></span>
                        </div>
                        <ul class="categories clearfix">
                            <li>
                                <select class="form-control" id="stajyertype">
                                    <option value="" disabled selected>Stajyer Tipini Seçiniz</option>
                                    <option value="1">Stajyer Eczacı</option>
                                    <option value="2">Yardımcı Eczacı</option>
                                    <option value="3">İkinci Eczacı</option>
                                </select>
                            </li>
                        </ul>
                    </div>
                    <div class="single-sidebar">
                        <div class="sec-title">
                            <h3>Eczane Kategorisi</h3>
                            <span class="border"></span>
                        </div>
                        <ul class="categories clearfix">
                            <li>
                                <select class="form-control" id="yilSeciniz">
                                    <option value="" disabled selected>Eczane Tipi Seçiniz</option>
                                    <option value="1">Ünite</option>
                                    <option value="2">Cadde</option>
                                    <option value="3">AVM</option>
                                    <option value="4">Semt</option>
                                </select>
                            </li>
                        </ul>
                    </div>
                    <div style="display:flex; justify-content:center;">
                        <a class="thm-btn bg-clr1" style="padding:8px; width:100%;" type="submit" href="/JobAdvert/Detail/2">ARA</a>
                    </div>
                </div>
            </div>
            <div class="col-lg-9 col-md-8 col-sm-12 col-xs-12">
                <div class="shop-content">
                    <div class="row advertpadding">
                        @if (Model != null)
                        {
                            foreach (var item in Model)
                            {
                                @await Html.PartialAsync("_AdvertComponentPartial", item)
                            }
                        }

                    </div>
                    <!--Start post pagination-->
                    @if (Model?.Count > 0)
                    {
                        @await Html.PartialAsync("_PaginationPartial", pagingVm)
                    }
                    <!--End post pagination-->
                </div>
            </div>

            <!--End Sidebar Wrapper-->
        </div>
    </div>
</section>
<!--End blog area-->
<style>
    .btn {
        text-align: left;
        width: 100%;
        min-height: 50px;
    }

    .dropdown-menu {
        width: 100%;
        padding-left: 10px;
    }

    .dropdown-menu {
        appearance: none;
    }
</style>